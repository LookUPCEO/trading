# [I] 라이브/shadow 상태 점검 (2026-06-28) — 중대 발견: 실주문 실행 100% 깨짐

**점검만 (설정/코드 변경 X). 판정: shadow(감쇠 측정기)는 건강·정상 누적 중. 그러나 실거래 데몬은 신호 3건 전부 주문 실패(버그 2개) — 실주문 0은 '신호 대기'가 아니라 '신호 왔으나 실행 불가'였음. 돈 손실 0(failed safe). 6/25 long fup0.733(우리 핵심 edge) 포함 3건 놓침.**

## 작업1 — 두 데몬 생존 ✅
- PID 14414 shadow: 가동 **20d 21h**, CPU 1.9%/MEM 2.1%, 정상. shadow.log 에러 0.
- PID 49338 live: 가동 **11d 18h**, CPU 2.0%/MEM 2.3%, 살아있음. KILL 파일 없음. **단 주문 실패 로그 다수(작업2).**

## 작업2 — 실거래 데몬: ★ 주문 실행 깨짐 (중대) ★
- ARM 상태 정상: `DRY=False ARM=True equity=$188.32 CAP/trade=$60` (실거래 ARMED 확인).
- **신호는 발화함** — 11일간 3건:
  | 시각(KST) | day/min | dir | fup | entry |
  |---|---|---|---|---|
  | 6/19 22:46 | min825 | -1(숏) | 0.272 | 1705.30 |
  | 6/25 23:17 | min856 | +1(롱) | 0.733 | 1551.27 |
  | 6/28 17:39 | min518 | -1(숏) | 0.297 | 1583.91 |
- **3건 전부 주문 실패 → ledger 0건, positions.json 없음, 실잔고 $188.32 불변(체결 0 = failed safe).**
- **버그 #2 (치명, 실패 직접 원인)**: `i_live_daemon.py:50 qty=round(notional/price,3)` → 0.035/0.039/0.038. Bybit ETHUSDT 퍼프 **qtyStep=0.01** 위반 → `code=10001 Qty invalid` 4회 재시도 후 `[fail] 안전 중단 필요`. **수정 = round(.,2)** (0.04, 명목 ~$62-68).
- **버그 #1 (잠복)**: `i_live_order.py:30 self.c.get_order_by_link()` 호출 — 실 `BybitClient`(execution.py:141)엔 이 메서드 **없음**(MockBybitClient:101 에만 있음) → 매 호출 AttributeError(catch, 비치명). 멱등 상태조회 무력화(재전송 중복 방어 X).
- **근본 원인 = 리허설-실거래 괴리**: I.13 리허설 12/12 PASS 는 **Mock 이 get_order_by_link 보유 + qty step 미검증**이라 통과. CLAUDE.md "흥분 금지 — 모든 promising 이 audit 에서 죽었다" 교훈 — Mock PASS 가 거짓 확신.
- 안전장치는 정상 작동: check_can_enter 통과(거부 0), 실패를 성공 오인 안 함(미기록), KILL 안 걸림(reject=False라 계속 가동=다음 신호 받음), reconcile 무이벤트 조용(불일치 0).

## 작업3 — shadow 수집 ✅ (감쇠 측정기 건강)
- state.json 최신 pending = 6/28 min518(최종수정 17:39 KST, 20분 전) — 능동 추적 중. big_thr 2.88(I.23 갱신값).
- outcomes **7줄(고유 6건, 6/25 min856 19ms 내 중복 1)**. 수집 공백 없음(연속).

## 작업4 — outcome 6건 내용 (n 무의미, 기록 확인용)
| day/min | dir | fup | gross_bp | net_TT | 방향맞음 |
|---|---|---|---|---|---|
| 6/09 m578 | 롱 0.70 | +1.5 | **−9.5** | ○(미미) |
| 6/09 m984 | 롱 0.70 | +158.85 | **+147.85** | ○ |
| 6/19 m825 | 숏 0.27 | +27.21 | **+16.21** | ○(숏 적중) |
| 6/25 m856 | 롱 0.72 | +95.53 | **+84.53** | ○ |
| 6/25 m1010 | 롱 0.70 | −22.74 | **−33.74** | ✗ |
| 6/25 m1023 | 롱 0.70 | +7.57 | **−3.43** | ○(gross<fee) |
- **방향 hit 5/6=0.83** (gross>0), net 양수 3/6, **평균 net +33.7bp/trade**. 백테스트 hit 0.68 과 어긋남 없음(n=6, 통계 무의미 — "기록되고 있나"만 확인, 감쇠 징후 없음). 큰 승 +147.85 가 평균 지배.
- 참고: shadow 는 양방향 thr0.70 추적(숏 포함) — 숏 6/19 +16.21 적중이나 n=1(25~28 숏 edge 0 결론과 별개, 추적은 정상).

## 작업5 — 머신 상시 가동
- **sleep 비활성** (`sleep 0`, `displaysleep 0`, `disksleep 0`) — 잠들지 않음 ✅. womp 1(네트워크 wake).
- **자동 재시작 없음** (LaunchAgent/cron 0) — 크래시/재부팅 시 수동 재기동 필요 ⚠️ (단 11~20일 무중단=안정).

## 판정
1. **shadow 감쇠 측정기 = 건강·정상 누적** (6건, hit 0.83, 공백 0). 감쇠 판정의 주 도구는 정상. 단 n=6 통계 무의미.
2. **실거래 데몬 = 신호 정상 발화하나 주문 실행 100% 깨짐(버그 2)** — 실주문 0의 진짜 이유. **돈 손실 0(failed safe)** 이나 6/25 롱 fup0.733(핵심 edge) 포함 3건 실거래 누락. 실거래 감쇠 측정은 0 진행.
3. **조치 필요(별도 확인 후)**: ① `i_live_daemon.py:50` qty round(.,2) + step floor ② `BybitClient.get_order_by_link` 추가(또는 _query 우회) ③ 리허설을 실 step/메서드 검증으로 강화 ④ 수정 후 재arm. **점검 단계라 미수정 — 사용자 확인 대기.**
4. 머신 sleep 무위험, 단 auto-restart 부재는 알아둘 것.
