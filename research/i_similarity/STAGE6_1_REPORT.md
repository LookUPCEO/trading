# [I] 6-1 — 전향 검증 인프라 + 능동청산 병행 보고서

**날짜**: 2026-06-07 · **코드**: `i_active_exit.py`(A), `i_shadow.py`+`i_shadow_daemon.py`(B)

## A — 능동청산 백테스트: **사전등록 5규칙 전부 고정 hold 에 패배 (깨끗한 negative)**
- 대상: 4h thr70 이벤트 79건. 매분 재예측 = 그 분의 21차원 상태로 kNN vote (분 m 末 정보만).
- **trailing 함정 회피**: 청산 결정 정보시점 ≤ 청산시점, 청산가 = mid[m] (진입 관행과 동일 — 동등 취급). fee 동일 T+T 11bp (조기청산도 taker) → 비교는 순수 경로 차이.
- 결과 (day-mean net bp, 95% CI):

| 규칙 | net | 발동률 | 보유 med |
|---|---|---|---|
| **FIXED 4h hold** | **+74.7** [+28.1,+123.9] | — | 240분 |
| R1 반전강(30m vote 반대≥0.65) | +52.8 | 46% | 240분 |
| R3 반전중(4h vote 반대≥0.6) | +45.0 | 68% | 118분 |
| R5 익절 +100bp | +34.1 | 61% | 129분 |
| R2 vote소멸+이익 | +32.0 | 94% | 31분 |
| R4 손절 -50bp | +12.0 | 65% | 92분 |

- 시기 전/후반 분해 모두 FIXED 최고. 5/5 전부 보고 (cherry-pick 없음).
- **why**: lean = 고변동 순간 (5단계) → 경로가 출렁이며 중간 트리거를 다 건드림. 신호의 가치는 "4h 끝까지" (winners run). 손절이 최악 — 우상향 꼬리를 자름. **이 규칙공간에서 ❌** (다른 청산공간 형제 ⬜ 남으나 방향성: 들고 가라). 일수익 개선 실패 — 고정 hold 유지.

## B — 전향(shadow) 검증 인프라: **가동 시작** ✅
- 구조: WS(orderbook.50+publicTrade) → 당일 초 그리드 → 매분 day-so-far 라벨 (**i_labeling 검증 함수 그대로** — truncation invariance 가 "day-so-far 재계산 = 배치 동일" 을 보장) → 동결 artifact (DB 1,135,331×21 + whitening 2023 + 정규화 통계 + big_thr) → kNN fup240 → thr70 신호 + 4h 후 outcome 기록. **돈 X, 기록만.**
- **백테스트 = 실시간 일치 점검 (4h-direction 게이트 방식)**: 2026-03-15 replay (asof artifact) —
  - incremental(분 단위 truncation) vs 배치 라벨: **max|Δ| = 0.00e+00 PASS**
  - 정규화 z: **max|Δ| = 0.00e+00**, fup240: **max|Δ| = 0.00e+00**, 신호일치 **1.000** → **완전 일치 (bit-identical)**.
- 신호 조건 = 백테스트와 동일 분포: min_of_day ∈ [480,1199], votes ≥ 70, fup ≥0.70/≤0.30. 진입가 = 분 close mid (백테스트 관행 동일).
- 가동: PID 기록, `research/i_similarity/shadow/logs/` 에 매분 jsonl + `outcomes.jsonl`. 당일 1Hz OB/trades 를 기존 스키마로 persist → **4/30 중단됐던 수집 재개 겸용** (big_thr 전일 q95 자동갱신).
- **정직 caveats**:
  - 정규화 통계 = 가용 마지막 90일 (2026-01-31~04-30) — **~5주 stale** (수집 공백 5/1~6/6). artifact 에 기록. 데이터 쌓이면 재생성 가능.
  - 수집 공백 5/1~6/6 은 pool 에도 없음 — 백필 옵션 (Bybit/Tardis 과거 데이터) 추후.
  - 시작일(6/7)은 warm (day 처음부터 못 봄) — **유효 신호는 다음 UTC 자정 이후 min 480 부터**.
  - 기대 빈도: 4h thr70 ≈ 0.09건/일 (79/851) → **통계적 결론까지 수개월** — 세팅+기다림 (이번 목적).
  - 머신 sleep 시 데몬 중단 — 상시 가동 환경 필요 (사용자 결정 사항).

## 종합
- **능동청산 길: 이 규칙공간엔 없음** — 4h 고정 hold 가 최선 (구조적 이유 명확).
- **전향 검증: 시작됨** — 파이프라인 완전 일치(0.0) 검증 후 가동. 2025Q3+ 미확정의 해소는 이제 시간이 담당.
