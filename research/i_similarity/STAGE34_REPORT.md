# [I] 34단계 — 21단계(진짜) vs 31단계(환상) 충돌 정면 해소 보고서

**날짜**: 2026-06-28 · **코드**: i_reconcile.py · **판정: 31단계가 맞고 21단계가 과대평가였음 — 단 "전부 가짜"가 아니라 "작은 표본이 부풀린 진짜 작은 edge". 같은 4h thr0.70 신호를 세 방식으로 재니: A stride-10 무dedup(stage5 원본) n=79 hit 0.684 day-net+74.7 CI[+28,+123] 0제외 5/5폴드 / B dense 무dedup(매분, 같은 신호 11배 표본) n=894 hit 0.556 +13.7 CI[−3,+30] 0포함 3/5 / C dense earliest(라이브 실제) n=409 hit 0.516 −6.5 CI[−24,+11] 0포함 3/5. → stage5 "5/5·CI 0제외"는 n=79 소표본 행운, 표본 11배 늘리면 hit 0.684→0.556·CI 0 포함·폴드 3/5 로 무너짐. 라이브 방식(earliest)은 더 낮음(≈본전). 21 동안 못 잡은 이유: 전 단계가 stride-10 + 무dedup(겹침 과다계상) 일관 사용 → 내부적으로 안 드러남, 30단계서 "라이브 실제 진입(earliest)" 물으니 노출. shadow 우호적=B(무dedup) 모집단+소표본(C와 모순 아님). 결론: 0.084%는 과대, 진짜 거래가능 edge ≈본전(hit 0.52~0.56 약우위 실재하나 fee와 상쇄, CI 0 포함).**

## 작업1·3 — 같은 신호, 세 측정 (핵심: stage5 폴드를 라이브 방식으로 재검)
| 측정 | n | 일수 | hit | 거래당 | day-net | CI | 반기폴드 |
|---|---|---|---|---|---|---|---|
| **A** stride-10 무dedup (stage5 원본) | 79 | 62 | **0.684** | +90.5 | +74.7 | **[+28,+123] 0제외** | **5/5 양수** |
| **B** dense 무dedup (매분, 같은 신호) | 894 | 356 | 0.556 | +34.8 | +13.7 | [−3,+30] 0포함 | 3/5 |
| **C** dense earliest (라이브 실제) | 409 | 356 | 0.516 | −10.4 | −6.5 | [−24,+11] 0포함 | 3/5 |
- **stage5 의 "5/5·CI 0제외·hit 0.684"는 A(stride-10, n=79 소표본)였음.** 같은 신호·같은 기간을 **매분(11배 표본)으로 재니 hit 0.684→0.556, CI 0 포함, 폴드 5/5→3/5** 로 무너짐. 라이브 방식(C earliest)은 더 낮음.
- 즉 **21단계 검증은 "틀린 방법"이 아니라 "작은 표본(79건)이 우연히 높게 나온 것"** — Bonferroni/마찰/audit 다 통과해도 n=79 hit 0.684 자체가 행운 추출이었음(표본 늘리면 0.556 로 회귀).

## 작업2 — shadow vs 31 백테 정면 (모순 해소)
- **shadow 기록데몬(14414)**: 매분 fup≥0.70 다 기록(블록 없음) = **B 무dedup 모집단**(day-net +13.7, 우호적).
- **라이브(54043)**: MAX_CONCURRENT=1 첫 신호만 = **C earliest**(≈본전).
- → shadow 우호적 6~7건 = B 모집단에서 뽑힌 소표본. **31 백테(C earliest)와 모순 아님** — 다른 모집단(무dedup vs earliest)을 본 것. shadow=실측이나 "겹침 포함" 모집단이라 라이브 단일포지션보다 우호적으로 보임.

## 작업4 — 거래당 vs 일가중 (−4.1/−10.4 vs +5.3 정체)
- C earliest: 거래당 평균 −10.4(꼬리손실 영향) vs 일평균 −6.5(날 동일가중). 둘 다 음수 점추정, CI 0 포함.
- **방향 hit 0.516(>0.5) = 약한 우위 실재** — net p50 −8(중앙도 약음수, fee 직후). 신호는 진짜지만 fee 못 넘김.

## 판정 (쉽게)
1. **누가 맞나 → 31이 맞습니다. 21이 과대평가했습니다.** 단 "21이 전부 가짜"가 아니라 **"작은 표본(79건)이 진짜 작은 edge 를 크게 보이게 했다"**. 같은 신호를 11배 표본으로 재면 +90.5→+13.7(무dedup)→−6.5(라이브)로 줄고 CI 가 0 을 포함합니다.
2. **0.084% 진짜인가 환상인가 → 과대평가였습니다.** 진짜 거래가능 edge 는 ≈본전(방향 52~56% 약우위는 실재하나 fee 와 상쇄). 0.084% 라는 큰 숫자는 소표본+측정방식(stride-10·무dedup 겹침)이 부풀린 것.
3. **왜 21 동안 못 잡았나**: 1~29단계가 전부 같은 측정(stride-10 + 무dedup)을 썼습니다. 그래서 내부적으로 모순이 안 드러났습니다. **30단계서 "라이브가 실제로 어떻게 진입하나(earliest, 한 번에 하나)"를 처음 물었을 때 노출**됐습니다. 측정이 라이브 실행(단일 포지션)과 안 맞았던 것입니다.
4. **31 "환상"은 맥락 있는 결론입니다** — 잘못된 신호가 아니라, 측정을 라이브 실제로 바꾸니 드러난 진실입니다. 다만 "환상(≈0 무작위)"보다 **"본전(약한 진짜 방향우위가 fee 와 상쇄)"**이 정확합니다.
5. **본인 의심은 정당했습니다** — 맥락 없는 뒤집힘처럼 보였고, 그 맥락이 이것입니다: 21은 소표본 stride-10 을, 31은 매분 라이브-실제(earliest)를 쟀고, **신호는 같지만 정직하게 잴수록 작아집니다**. shadow 가 우호적인 건 "겹침 포함" 모집단이라 그렇고, 라이브 단일포지션은 ≈본전입니다.
- **운영점 불변**(고정 4h earliest, PID 54043). **유일 판정 = 라이브 실측 누적**(이제 어느 측정이 진실인지 알았으니, 라이브 단일포지션 실체결이 곧 진짜 값). 레버리지=본전 증폭=위험만(재확인). 산출물: STAGE34_REPORT.md, i_reconcile.py.
